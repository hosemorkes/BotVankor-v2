# Telegram & MAX бот для Vankor

Бот для работы с расчётом зарплаты и получением информации о погоде.

## 🚀 Быстрый старт

### Установка зависимостей

```bash
pip install -r requirements.txt
```

### Настройка переменных окружения

1. Скопируйте `.env.example` в `.env`:
```bash
cp .env.example .env
```

2. Заполните необходимые переменные в `.env`:
- `TELEGRAM_BOT_TOKEN` - токен вашего Telegram бота (получить у @BotFather)

### Запуск

```bash
python -m app.main
```

## 🐳 Docker

### Сборка образа

```bash
docker-compose build
```

### Запуск

```bash
docker-compose up -d
```

## 📁 Структура проекта

```
project_root/
├── app/
│   ├── main.py                # Точка входа
│   ├── bot/                   # Обработчики ботов
│   │   ├── telegram/          # Telegram обработчики
│   │   └── max/               # MAX обработчики
│   ├── services/              # Бизнес-логика
│   └── database/              # Работа с БД
├── data/                      # Данные (БД)
├── tests/                     # Тесты
└── requirements.txt           # Зависимости
```

## 🧪 Тестирование

```bash
pytest tests/
```

## 📝 Разработка

Проект следует правилам из `.cursorrules`:
- Чистый, понятный код
- Типизация везде
- Async где уместно
- Тестируемая бизнес-логика

## 📄 Лицензия

MIT

