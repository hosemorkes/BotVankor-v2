# Telegram & MAX бот для Vankor

Бот для работы с расчётом зарплаты и получением информации о погоде.

## 🚀 Быстрый старт

### 1. Активация виртуального окружения

**Windows (PowerShell):**
```powershell
.\venv\Scripts\Activate.ps1
```

**Linux/Mac:**
```bash
source venv/bin/activate
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Настройка переменных окружения

1. Скопируйте `.env.example` в `.env`:
```bash
# Windows (PowerShell)
Copy-Item .env.example .env

# Linux/Mac
cp .env.example .env
```

2. Откройте `.env` и заполните токен:
```
TELEGRAM_BOT_TOKEN=ваш_токен_бота_здесь
```

**Как получить токен:**
- Найдите [@BotFather](https://t.me/BotFather) в Telegram
- Отправьте команду `/newbot`
- Следуйте инструкциям и получите токен

### 4. Запуск бота

```bash
python -m app.main
```

После запуска вы увидите в консоли:
```
INFO - Бот запущен
```

Теперь бот готов к работе! Отправьте ему `/start` в Telegram.

## 🐳 Docker

### Сборка образа

```bash
docker-compose build
```

### Запуск

```bash
docker-compose up -d
```

## 📁 Структура проекта

```
project_root/
├── app/
│   ├── main.py                # Точка входа
│   ├── bot/                   # Обработчики ботов
│   │   ├── telegram/          # Telegram обработчики
│   │   └── max/               # MAX обработчики
│   ├── services/              # Бизнес-логика
│   └── database/              # Работа с БД
├── data/                      # Данные (БД)
├── tests/                     # Тесты
└── requirements.txt           # Зависимости
```

## 🧪 Тестирование

```bash
pytest tests/
```

## 📝 Разработка

Проект следует правилам из `.cursorrules`:
- Чистый, понятный код
- Типизация везде
- Async где уместно
- Тестируемая бизнес-логика

## 📄 Лицензия

MIT

