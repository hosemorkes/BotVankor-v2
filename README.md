# Telegram & MAX бот для Vankor

Бот для работы с расчётом зарплаты и получением информации о погоде.

## ✅ Статус проекта

### Выполненные этапы:

- ✅ **ЭТАП 1** - Минимальный живой бот
  - Реализованы команды `/start` и `/help`
  - Настроена работа с Telegram API
  - Бот успешно запускается и отвечает пользователям

- ✅ **ЭТАП 2** - Регистрация пользователей
  - Команда `/start` сохраняет пользователей в БД
  - Сохраняются: `telegram_id`, `username`, `first_name`, `last_name`
  - Повторный `/start` не создаёт дубликаты
  - Все входы пользователей логируются

- ✅ **ЭТАП 3** - Стабилизация базы данных
  - Надёжное подключение к SQLite
  - Автоматическое создание таблиц при старте
  - Обработка ошибок БД с логированием
  - CRUD-операции для всех моделей
  - Полное покрытие тестами (9 тестов БД)

- ✅ **ЭТАП 4** - Погода Ванкорского месторождения
  - Интеграция с OpenWeatherMap API
  - Команда `/weather` для получения актуальной погоды
  - Координаты Ванкора настраиваются через `.env`
  - Кэширование данных на 10 минут
  - Красивое форматирование ответа с эмодзи
  - Обработка ошибок API и сети

- ✅ **ЭТАП 5** - Расчёт зарплаты (вахтовый метод)
  - Полный расчёт зарплаты по вахтовому методу работы
  - Учёт ночных смен, простоя, дней в пути, праздников
  - Региональные и северные надбавки
  - Сохранение расчётов в БД
  - Детальный отчёт с разбивкой по статьям

- ✅ **ЭТАП 6** - Расчёт 13-й зарплаты (годовое вознаграждение)
  - Расчёт годовой премии (13-й зарплаты)
  - Учёт месяцев работы в компании
  - Коэффициенты KPI и корректирующие коэффициенты
  - Региональные и северные надбавки на годовую премию
  - Детальный отчёт с данными по каждому месяцу

- ✅ **ЭТАП 7** - Расширенная информация о погоде
  - Прогноз погоды на 7 дней
  - Анализ вероятности вылета вертолёта на 3 дня
  - Анализ погодных условий для маршрута Ванкор ↔ Игарка
  - Система баллов риска с юмористическими комментариями

### Текущая функциональность:

- 📋 Команды: `/start`, `/help`, `/weather`
- 👥 Регистрация и сохранение пользователей в БД
- 💰 Расчёт зарплаты по вахтовому методу работы
- 🎁 Расчёт 13-й зарплаты (годового вознаграждения)
- 🌤️ Получение актуальной погоды для Ванкорского месторождения
- 📅 Прогноз погоды на 7 дней
- 🚁 Анализ вероятности вылета вертолёта на основе погоды
- 💾 SQLite база данных с моделями: User, SalaryRecord, WeatherRecord
- 🧪 Покрытие тестами: 21 тест (все проходят)

## 🚀 Быстрый старт

### 1. Активация виртуального окружения

**Windows (PowerShell):**
```powershell
.\venv\Scripts\Activate.ps1
```

**Linux/Mac:**
```bash
source venv/bin/activate
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Настройка переменных окружения

1. Скопируйте `.env.example` в `.env`:
```bash
# Windows (PowerShell)
Copy-Item .env.example .env

# Linux/Mac
cp .env.example .env
```

2. Откройте `.env` и заполните токены:
```
TELEGRAM_BOT_TOKEN=ваш_токен_бота_здесь
WEATHER_API_KEY=ваш_api_ключ_погоды_здесь

# Координаты Ванкорского месторождения (опционально, есть значения по умолчанию)
VANKOR_LATITUDE=69.5
VANKOR_LONGITUDE=88.0
VANKOR_NAME=Ванкорское месторождение

# Координаты Игарки (для анализа вероятности вылета, опционально)
IGARKA_LATITUDE=67.4
IGARKA_LONGITUDE=86.6
IGARKA_NAME=Игарка
```

**Как получить токены:**

**Telegram Bot Token:**
- Найдите [@BotFather](https://t.me/BotFather) в Telegram
- Отправьте команду `/newbot`
- Следуйте инструкциям и получите токен

**Weather API Key (OpenWeatherMap):**
- Зарегистрируйтесь на [OpenWeatherMap](https://openweathermap.org/api)
- Перейдите в раздел API Keys
- Создайте бесплатный API ключ (Free tier)
- Скопируйте ключ в `.env`

### 4. Запуск бота

```bash
python -m app.main
```

После запуска вы увидите в консоли:
```
INFO - Бот запущен
```

Теперь бот готов к работе! Отправьте ему `/start` в Telegram.

## 🐳 Docker

### Сборка образа

```bash
docker-compose build
```

### Запуск

```bash
docker-compose up -d
```

### Просмотр логов

```bash
docker logs vankor_bot
```

Или в реальном времени:

```bash
docker logs -f vankor_bot
```

### Остановка

```bash
docker-compose down
```

### 🚀 Развертывание на Linux сервере

#### Предварительные требования

Убедитесь, что на сервере установлены:
- Docker (версия 20.10+)
- Docker Compose (версия 2.0+)

Проверка установки:
```bash
docker --version
docker-compose --version
```

#### Пошаговая инструкция

**1. Создание директории для проекта**

```bash
# Создаём директорию для проекта (например, в домашней директории "root")
mkdir -p ~/botvankor
cd ~/botvankor
```

Или в системной директории:
```bash
sudo mkdir -p /opt/botvankor
cd /opt/botvankor
sudo chown $USER:$USER /opt/botvankor
```

**2. Клонирование репозитория**

```bash
# Клонируем репозиторий из GitHub
git clone https://github.com/hosemorkes/BotVankor-v2.git .

# Или если директория уже существует:
git clone https://github.com/hosemorkes/BotVankor-v2.git botvankor
cd botvankor
```

**3. Настройка переменных окружения**

```bash
# Копируем шаблон .env файла
cp .env.example .env

# Редактируем .env файл и заполняем токены
nano .env
# или
vim .env
```

Заполните обязательные переменные:
```
TELEGRAM_BOT_TOKEN=ваш_токен_бота_здесь
WEATHER_API_KEY=ваш_api_ключ_погоды_здесь
```

**4. Сборка Docker образа**

```bash
# Собираем образ
docker-compose build
```

**5. Запуск контейнера**

```bash
# Запускаем в фоновом режиме
docker-compose up -d
```

**6. Проверка работы**

```bash
# Проверяем статус контейнера
docker-compose ps

# Просматриваем логи
docker logs vankor_bot

# Или в реальном времени
docker logs -f vankor_bot
```

**7. Автозапуск при перезагрузке сервера**

Контейнер уже настроен на автозапуск (`restart: unless-stopped` в docker-compose.yml), поэтому при перезагрузке сервера он автоматически запустится.

Для проверки:
```bash
# Перезагружаем сервер
sudo reboot

# После перезагрузки проверяем статус
docker-compose ps
```

#### Полезные команды для управления

```bash
# Остановка контейнера
docker-compose stop

# Запуск остановленного контейнера
docker-compose start

# Перезапуск контейнера
docker-compose restart

# Остановка и удаление контейнера
docker-compose down

# Пересборка и перезапуск
docker-compose up -d --build

# Просмотр логов последних 100 строк
docker logs --tail 100 vankor_bot

# Просмотр использования ресурсов
docker stats vankor_bot
```

#### Обновление бота

```bash
# Переходим в директорию проекта
cd ~/botvankor  # или /opt/botvankor

# Получаем последние изменения из репозитория
git pull

# Пересобираем и перезапускаем контейнер
docker-compose up -d --build

# Проверяем логи
docker logs -f vankor_bot
```

#### Расположение файлов на сервере

После развертывания файлы будут находиться:

```
~/botvankor/                    # или /opt/botvankor/
├── .env                        # Файл с переменными окружения (создайте из .env.example)
├── data/                       # База данных (создаётся автоматически)
│   └── bot.db                  # SQLite база данных
├── docker-compose.yml          # Конфигурация Docker Compose
├── Dockerfile                  # Образ Docker
└── app/                        # Код приложения
```

**Важно:**
- Файл `.env` содержит секретные данные, не коммитьте его в Git
- Папка `data/` содержит базу данных, делайте резервные копии
- Логи доступны через `docker logs vankor_bot`

### Расположение файлов в Docker

**Внутри контейнера:**
- Код приложения: `/app/app/`
- База данных: `/app/data/bot.db`
- Файл переменных окружения: `/app/.env` (монтируется только для чтения)
- Рабочая директория: `/app/`

**На хосте (вне контейнера):**
- База данных: `./data/bot.db` (монтируется как volume, доступна с хоста)
- Файл переменных окружения: `./.env` (монтируется как volume только для чтения, доступен с хоста)

**Важно:**
- Папка `data/` монтируется как volume, поэтому все изменения БД сохраняются на хосте
- Файл `.env` монтируется только для чтения (`:ro`), чтобы предотвратить случайное изменение из контейнера
- Переменные окружения загружаются через `env_file` в docker-compose.yml
- Логи выводятся в stdout/stderr и доступны через `docker logs`

## 📁 Структура проекта

```
project_root/
├── app/
│   ├── main.py                # Точка входа
│   ├── bot/                   # Обработчики ботов
│   │   ├── telegram/          # Telegram обработчики
│   │   │   ├── handlers.py    # Обработчики команд и сообщений
│   │   │   └── keyboards.py  # Inline клавиатуры
│   │   └── max/               # MAX обработчики
│   ├── services/              # Бизнес-логика
│   │   ├── salary_service.py           # Расчёт зарплаты (вахтовый метод)
│   │   ├── annual_bonus_service.py     # Расчёт 13-й зарплаты
│   │   ├── weather_service.py          # Текущая погода
│   │   ├── seven_day_weather.py        # Прогноз погоды на 7 дней
│   │   └── flight_forecast_weather.py  # Анализ вероятности вылета
│   └── database/              # Работа с БД
│       └── db.py              # ORM модели + CRUD + инициализация
├── data/                      # Данные (БД)
├── tests/                     # Тесты
└── requirements.txt           # Зависимости
```

## 🧪 Тестирование

### Запуск всех тестов:
```bash
pytest tests/ -v
```

### Запуск тестов по категориям:
```bash
# Тесты базы данных
pytest tests/test_db.py -v

# Тесты расчёта зарплаты
pytest tests/test_salary.py -v

# Тесты погоды
pytest tests/test_weather.py -v
```

### Результаты тестирования:
- ✅ 9 тестов базы данных
- ✅ 3 теста расчёта зарплаты
- ✅ 9 тестов погоды
- **Всего: 21 тест (все проходят)**

## 📝 Разработка

Проект следует правилам из `.cursorrules`:
- Чистый, понятный код
- Типизация везде
- Async где уместно
- Тестируемая бизнес-логика

## 🔧 Технические детали

### Используемые технологии:
- **Python 3.13+** - язык программирования
- **python-telegram-bot 22.5** - библиотека для работы с Telegram API
- **SQLAlchemy 2.0.45** - ORM для работы с базой данных
- **aiohttp 3.13.2** - асинхронный HTTP клиент
- **pytest 7.4.3** - фреймворк для тестирования

### Особенности реализации:
- **Асинхронная архитектура** - все запросы к API выполняются асинхронно
- **Кэширование погоды** - данные кэшируются для снижения нагрузки на API:
  - Текущая погода: 30 минут
  - Прогноз на 7 дней: 60 минут
  - Анализ вылета: 60 минут
- **Обработка ошибок** - все ошибки логируются и обрабатываются корректно
- **Конфигурация через .env** - координаты и ключи настраиваются через переменные окружения
- **Безопасная работа с БД** - контекстные менеджеры для гарантированного закрытия сессий
- **Точные финансовые расчёты** - использование Decimal для избежания ошибок округления

## 📚 Описание сервисов

### 💰 salary_service.py
Сервис для расчёта зарплаты по вахтовому методу работы.

**Основные функции:**
- `calculate_salary()` - рассчитывает зарплату с учётом всех факторов
- `format_salary_report()` - форматирует отчёт для пользователя

**Учитываемые факторы:**
- Часовая ставка и отработанные дни
- Ночные смены (доплата 40%)
- Дни простоя (оплата 2/3 от ставки)
- Дни в пути (8 часов в день)
- Праздничные дни
- Премия месячная (33%)
- Региональная надбавка (0-100%)
- Северная надбавка (0-100%)
- Налог 13% (с учётом льгот)

### 🎁 annual_bonus_service.py
Сервис для расчёта 13-й зарплаты (годового вознаграждения).

**Основные функции:**
- `calculate_annual_bonus()` - рассчитывает годовую премию
- `format_annual_bonus_report()` - форматирует отчёт для пользователя

**Особенности расчёта:**
- Учёт дней работы по каждому месяцу года
- Среднемесячная база для расчёта
- Целевой процент годового вознаграждения по должности
- Коэффициенты KPI и корректирующие коэффициенты
- Региональные и северные надбавки на годовую премию
- Налог 13%

### 🌤️ weather_service.py
Сервис для получения текущей погоды для Ванкорского месторождения.

**Основные функции:**
- `get_weather()` - получает актуальную погоду через OpenWeatherMap API
- `format_weather_report()` - форматирует отчёт с эмодзи и датой

**Особенности:**
- Кэширование на 30 минут
- Отображение температуры, влажности, давления, ветра, видимости
- Форматирование даты на русском языке
- Учёт часового пояса UTC+7

### 📅 seven_day_weather.py
Сервис для получения прогноза погоды на 7 дней.

**Основные функции:**
- `get_7_day_forecast()` - получает прогноз на 7 дней
- `format_7_day_forecast_report()` - форматирует отчёт по дням

**Особенности:**
- Использует OpenWeatherMap API `/forecast` endpoint
- Группирует прогнозы по дням (из данных каждые 3 часа)
- Показывает минимум, максимум и среднюю температуру
- Отображает "Сегодня" и "Завтра" для первых дней
- Кэширование на 60 минут

### 🚁 flight_forecast_weather.py
Сервис для анализа вероятности вылета вертолёта на основе прогноза погоды.

**Основные функции:**
- `get_flight_forecast()` - получает прогноз для анализа вылета
- `format_flight_forecast_report()` - форматирует анализ с рекомендациями

**Анализ погодных условий:**
- Получает прогноз на 3 дня для Ванкора и Игарки
- Анализирует ветер, облачность, осадки, температуру
- Вычисляет баллы риска по специальным правилам для Крайнего Севера

**Система баллов риска:**
- Ветер > 12 м/с → +3 балла
- Порывы > 15 м/с → +3 балла
- Облачность ≥ 80% → +2 балла
- Осадки (снег/сильный дождь) → +2 балла
- Температура ≤ -45°C → +3 балла (экстремальный холод для Крайнего Севера)

**Рекомендации:**
- ≤ 3 балла: ✅ Вылет возможен
- 4-6 баллов: ⚠️ Осторожно — шанс задержки
- ≥ 7 баллов: ❌ Вылет НЕ рекомендуется

**Особенности:**
- Адаптировано для условий Крайнего Севера
- Юмористические комментарии для каждого дня
- Детальный анализ погодных условий для обеих точек маршрута
- Кэширование на 60 минут

## 📄 Лицензия

MIT

